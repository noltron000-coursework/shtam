'use strict';

const hello = 'world';
console.log(hello);

// add(n)
// subtract(n)
// multiply(n)
// divide(n)
// split(n) - Returns an array of values use this to split a bill.
// new Currency(7).split(3) -> [2.33, 2.33, 2.34]
